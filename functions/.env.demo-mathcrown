# Local emulator values for firebase-functions params (defineString).
# Without this file the emulator prompts interactively for each param,
# which hangs headless/CI runs. Deployed values are set per-project.
TUTOR_MODEL=claude-sonnet-5
STRIPE_PRICE_PREMIUM=
STRIPE_PRICE_FAMILY=
STRIPE_PRICE_MAX=
CHECKOUT_ORIGIN=http://127.0.0.1:5000
